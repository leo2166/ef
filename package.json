{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp *.html *.css *.js *.json *.xml public/ && cp -r images icons css public/"
  }
}
